name: Backend CI

on:
  push:
    paths:
      - BackEnd/**
      - .github/workflows/backend-ci.yml
  pull_request:
    paths:
      - BackEnd/**
      - .github/workflows/backend-ci.yml

jobs:
  test:
    name: Run backend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: BackEnd
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Make Maven Wrapper executable
        run: chmod +x mvnw

      - name: Run Maven tests
        run: ./mvnw -B testname: Backend CI

on:
  push:
    paths:
      - BackEnd/**
      - .github/workflows/backend-ci.yml
  pull_request:
    paths:
      - BackEnd/**
      - .github/workflows/backend-ci.yml

jobs:
  test:
    name: Run backend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: BackEnd
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Run Maven tests
        run: ./mvnw -B test
